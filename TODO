Web frontend

Thread logs up to the web

Some art

Make a LuaExecutor object, and extract globals instead of return values?

Put lua in its own process?

Handle bad return types and exceptions in Lua
