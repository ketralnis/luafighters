Web frontend

Thread logs up to the web

Some art

Handle bad return types and exceptions in Lua
maybe also wrap user scripts in one that returns the logs and state correctly

Deal with stalemate

Limit number of concurrent games

surface API errors in the frontend