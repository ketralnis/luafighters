* Install Vagrant https://www.vagrantup.com/
* run `vagrant up`
* Point a browser to http://localhost:5000/

For development it can help to:

    vagrant ssh -c 'cd luafighters && make asciiplayer'

You may need to resize your terminal